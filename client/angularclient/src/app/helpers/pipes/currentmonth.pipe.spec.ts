import { CurrentmonthPipe } from './currentmonth.pipe';

describe('CurrentmonthPipe', () => {
  it('create an instance', () => {
    const pipe = new CurrentmonthPipe();
    expect(pipe).toBeTruthy();
  });
});
